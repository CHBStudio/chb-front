export const root = '/';
export const main = `${root}main`;
export const contact =  `${root}contact`;
export const technologies = `${root}technologies`;
export const projects = `${root}projects`;
export const workflow = `${root}workflow`;


export default {
    root,
    main,
    contact,
    technologies,
    projects,
    workflow,
}
